cat << a << b
Fecha quando o segundo eof é accionado durante o primeiro, nao esta a assumir como 2 heredocs

fazer os pipes ( | ) (Joao)


// TRANQUILO //

cat < a.txt (nao existe) -- still reachable

<< eof - feito mas da exit